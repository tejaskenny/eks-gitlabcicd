apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: famousdots
  labels:
    env: "prod"
    setup: api
spec:
  type: ClusterIP
  selector:
    env: "prod"
    setup: api 
  ports:
    - port: 80
      targetPort: 7080