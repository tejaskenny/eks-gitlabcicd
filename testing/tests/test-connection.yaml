apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "famousdots-chart.fullname" . }}-test-connection"
  labels:
    {{- include "famousdots-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "famousdots-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
