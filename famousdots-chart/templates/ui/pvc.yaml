apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: {{ .Chart.Name }}-pvc-nginx
    namespace: {{ .Release.Namespace }}
spec:
    accessModes:
        -   ReadWriteOnce
    resources:
        requests:
            storage: {{ .Values.ui.volume.storageSize }}
    storageClassName: {{ .Values.ui.volume.storageClassName }}