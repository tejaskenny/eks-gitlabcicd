apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-ui-service
  namespace: {{ .Values.namespace }}
  labels:
    env: {{ .Values.ui.config.ENV }}
spec:
  type: {{ .Values.ui.service.type }}
  selector:
    app: {{ .Chart.Name}}-ui
  ports:
    - port: {{ .Values.ui.service.port }}
      targetPort: {{ .Values.ui.service.targetPort }}