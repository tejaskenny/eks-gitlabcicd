apiVersion: v1 
kind: ConfigMap 
metadata:
  name: {{ .Values.ui.configMapName }}
  namespace: {{ .Release.Namespace }}
data:
  config.js: |
    window.APP_CONFIG = {
      API_BASE_URL: "{{ .Values.ui.apiBaseUrl }}"
    };